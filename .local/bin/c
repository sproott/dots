#!/usr/bin/env bash

if [ "$1" = "run" ] ; then
  RUN=true
  FILENAME=$2
else
  RUN=false
  FILENAME=$1
fi

FILENAME=${FILENAME:-main}
FILES=$(ls *.c)

cc -std=c99 -Wall -Wextra -Werror -g -o $FILENAME $FILES

if [ $? -eq 0 ] && $RUN ; then
  ./$FILENAME
fi

