#!/bin/sh

if [ "$#" -lt 1 ]; then
  echo >&2 'Usage: save_image_from_clipboard FILE'
  exit 1
fi

file="$1"

if [ -f "$file" ]; then
  echo >&2 'File already exists. Exitting...'
  exit 1
fi

xclip -selection clipboard -t image/png -o > "$file"
